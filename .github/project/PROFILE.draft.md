# AI-SDLC project profile — DRAFT

> Generated by `/bootstrap-project` on `<ISO-date>`.
> Review each field. Every field must be confirmed by a human before this is renamed to `PROFILE.md`.
> Until `PROFILE.md` exists, all AI-SDLC skills will abort with a helpful error.

## Confirmation checklist

- [ ] All `[NEEDS HUMAN INPUT]` entries replaced
- [ ] Evidence reviewed for each stack claim
- [ ] Starter guideline files filled in (`.github/project/guidelines/*.md`)
- [ ] Stack files filled in (`.github/project/stacks/*.md`)
- [ ] Quarantined paths listed (if any)
- [ ] Rename this file to `PROFILE.md` to activate

---

```yaml
project:
  name: <from-package.json-or-git-remote>
  type: <frontend | backend | fullstack | infra | fullstack-monorepo>
  repo_kind: <single | monorepo>

stacks:
  frontend:
    framework: <react | angular | vue | none>        # source: <file>:<line>
    meta_framework: <next | nuxt | vite | none>      # source: <file>:<line>
    language: <typescript | javascript>              # source: <file>:<line>
    styling: <tailwind | css-modules | ...>          # source: <file>:<line>
    test: <vitest | jest | none>                     # source: <file>:<line>
  backend:
    framework: <dotnet | express | fastify | django | none>   # source: <file>:<line>
    language: <csharp | typescript | python | ...>   # source: <file>:<line>
    orm: <ef-core | prisma | typeorm | none>         # source: <file>:<line>
    test: <xunit | jest | pytest | none>             # source: <file>:<line>
  contract:
    format: <openapi | graphql | shared-types | none>
    path: <contracts/openapi.yaml | contracts/schema.graphql | contracts/>
  infra:
    ci: <azure-pipelines | github-actions | gitlab-ci>
    iac: <bicep | terraform | none>
    container: <docker | none>

# Conventions automatically detected. Confirm each.
conventions_detected:
  - "<human-readable statement>"                     # source: <file>:<line>

# Which master skills apply to this project.
recommended_skills:
  always:
    - requirements-analysis
    - technical-slicing
    - implement-story
    - implement-slice
    - pr-review
    - security-review
    - tech-debt-review
  stack_specific: []                                 # e.g. [react-patterns, dotnet-patterns]
  not_applicable: []

# Commands the hooks and pipelines run.
gates:
  pre_commit: "<lint-and-quick-test command, e.g. pnpm lint && pnpm test --run>"
  pre_merge:  "<full-build-and-test command, e.g. pnpm build && dotnet test>"
  unit_test:  "<unit test command, e.g. pnpm test --run>"
  integration_test: "<integration test command, e.g. pnpm test:integration>"
  format:     "<formatter command, e.g. pnpm prettier --write>"

  # Compile checks — run after tests, before commit
  compile:
    frontend: "<e.g. pnpm build, or n/a if no frontend>"
    backend:  "<e.g. dotnet build, or n/a if no backend>"

  # Startup commands — run the app for verification
  startup:
    frontend: "<e.g. pnpm dev, or n/a>"
    backend:  "<e.g. dotnet run --project apps/api, or n/a>"

  # Healthchecks — must return exit 0 when app is up
  healthcheck:
    frontend: "<e.g. curl -sf http://localhost:3000 > /dev/null, or n/a>"
    backend:  "<e.g. curl -sf http://localhost:5000/health > /dev/null, or n/a>"

  # How long to wait between starting the app and hitting healthcheck
  startup_wait_seconds: 15

  # Set true to skip browser verification (e.g. headless backend project)
  skip_browser_verification: false

# Paths that github copilot must never auto-edit. Implementations that require these
# will be flagged for human pairing. Keep this list small and deliberate.
quarantined_paths: []

# Reviewers auto-added to design and feature PRs. Keep as ADO user IDs or emails.
reviewers: []
```
